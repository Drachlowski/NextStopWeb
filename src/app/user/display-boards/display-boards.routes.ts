import { Routes } from '@angular/router';
import { DisplayBoardComponent } from './display-board/display-board.component';

export const DISPLAY_BOARDS_ROUTES: Routes = [
  { path: '', component: DisplayBoardComponent }
];
